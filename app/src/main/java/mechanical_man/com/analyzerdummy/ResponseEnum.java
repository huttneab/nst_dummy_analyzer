package mechanical_man.com.analyzerdummy;

/**
 * Created by Mechanical Man on 11/20/17.
 */

public enum ResponseEnum {
    GAS_DETECTION(), STATIC_PRESSURE(), TRANSIENT_FLOW(),
    FLOW_RATE(), PRESSURE_DROP(), CONCENTRATION(), GAS_ZERO(), SENSOR_ZERO(), CANCEL(),
    READ_GAS(), SPAN_GAS(), READ_FLOW(), SPAN_FLOW(), READ_PRESSURE(), SPAN_PRESSURE(),
    READ_VACUUM(), SPAN_VACUUM(), CALIBRATION_DATE()
}
